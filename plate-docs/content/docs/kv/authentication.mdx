---
title: Authentication
description: Authenticate with Plate-KV using API keys
---

All API endpoints require authentication using the `Authorization` header with your API key.

## Header Format

```
Authorization: YOUR_API_KEY
```

## Request Methods

<CodeBlockTabs defaultValue="javascript" groupId="request-method">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">
      JavaScript
    </CodeBlockTabsTrigger>

    <CodeBlockTabsTrigger value="rust">
      Rust
    </CodeBlockTabsTrigger>

    <CodeBlockTabsTrigger value="go">
      Go
    </CodeBlockTabsTrigger>

    <CodeBlockTabsTrigger value="curl">
      cURL
    </CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/keys/mykey`, {
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      }
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .get(&format!("{}/{}/keys/mykey", base_url, plate_id))
            .header("Authorization", api_key)
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	req, _ := http.NewRequest("GET", fmt.Sprintf("%s/%s/keys/mykey", baseUrl, plateId), nil)
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X GET "[base-url]/[id]/keys/mykey" \
      -H "Authorization: YOUR_API_KEY"
    ```
  </CodeBlockTab>
</CodeBlockTabs>

## Error Responses

Missing or invalid authorization returns `401 Unauthorized`:

```json
{
  "error": "missing_authorization",
  "message": "authorization header is required"
}
```

```json
{
  "error": "invalid_authorization",
  "message": "authorization denied"
}
```

## Security Notes

- Keep your API keys secure and never expose them in client-side code
- Rotate keys periodically
- Use HTTPS in production environments
- Implement key validation at the application level
- Store keys in environment variables or secure secret management systems
