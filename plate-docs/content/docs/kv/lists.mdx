---
title: Lists API
description: Ordered collections for queues, stacks, feeds, and work items
---

Lists are ordered collections. They are useful for queues, stacks, feeds, and ordered work items.

## Endpoints

| Method | Endpoint | Description |
| --- | --- | --- |
| POST | `/{plateId}/lists/left/push` | Push to the left |
| POST | `/{plateId}/lists/right/push` | Push to the right |
| POST | `/{plateId}/lists/left/pop` | Pop from the left |
| POST | `/{plateId}/lists/right/pop` | Pop from the right |
| GET | `/{plateId}/lists/range/{key}` | Read a range |
| GET | `/{plateId}/lists/item/{key}/{index}` | Read one item |
| GET | `/{plateId}/lists/length/{key}` | Count items |
| POST | `/{plateId}/lists/position` | Find an item's position |
| POST | `/{plateId}/lists/set` | Replace an item by index |
| POST | `/{plateId}/lists/insert` | Insert before or after a pivot |
| POST | `/{plateId}/lists/remove` | Remove matching items |
| POST | `/{plateId}/lists/trim` | Trim list bounds |
| POST | `/{plateId}/lists/move` | Move between lists |
| POST | `/{plateId}/lists/command` | Execute allowed list commands |
| POST | `/{plateId}/lists/{key}/command` | Execute key-specific list commands |

## Examples

### Push to the right

<CodeBlockTabs defaultValue="javascript" groupId="lists-push">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/right/push`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "jobs",
        values: ["a", "b", "c"]
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/right/push", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "jobs",
                "values": ["a", "b", "c"]
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"jobs","values":["a","b","c"]}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/right/push", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/right/push" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "jobs",
        "values": ["a", "b", "c"]
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Pop from the left

<CodeBlockTabs defaultValue="javascript" groupId="lists-pop">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/left/pop`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "jobs",
        count: 2
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/left/pop", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "jobs",
                "count": 2
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"jobs","count":2}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/left/pop", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/left/pop" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "jobs",
        "count": 2
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Read a range

<CodeBlockTabs defaultValue="javascript" groupId="lists-range">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/range/jobs?start=0&stop=9`, {
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      }
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .get(&format!("{}/{}/lists/range/jobs?start=0&stop=9", base_url, plate_id))
            .header("Authorization", api_key)
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	req, _ := http.NewRequest("GET", fmt.Sprintf("%s/%s/lists/range/jobs?start=0&stop=9", baseUrl, plateId), nil)
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X GET "[base-url]/[id]/lists/range/jobs?start=0&stop=9" \
      -H "Authorization: YOUR_API_KEY"
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Insert relative to a pivot

<CodeBlockTabs defaultValue="javascript" groupId="lists-insert">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/insert`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "jobs",
        where: "before",
        pivot: "b",
        value: "urgent"
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/insert", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "jobs",
                "where": "before",
                "pivot": "b",
                "value": "urgent"
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"jobs","where":"before","pivot":"b","value":"urgent"}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/insert", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/insert" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "jobs",
        "where": "before",
        "pivot": "b",
        "value": "urgent"
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Move an item between lists

<CodeBlockTabs defaultValue="javascript" groupId="lists-move">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/move`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        source: "pending",
        destination: "processing",
        from: "right",
        to: "left"
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/move", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "source": "pending",
                "destination": "processing",
                "from": "right",
                "to": "left"
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"source":"pending","destination":"processing","from":"right","to":"left"}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/move", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/move" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "source": "pending",
        "destination": "processing",
        "from": "right",
        "to": "left"
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Get Item by Index

<CodeBlockTabs defaultValue="javascript" groupId="lists-item">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/item/jobs/0`, {
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      }
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .get(&format!("{}/{}/lists/item/jobs/0", base_url, plate_id))
            .header("Authorization", api_key)
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	req, _ := http.NewRequest("GET", fmt.Sprintf("%s/%s/lists/item/jobs/0", baseUrl, plateId), nil)
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X GET "[base-url]/[id]/lists/item/jobs/0" \
      -H "Authorization: YOUR_API_KEY"
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Get Length

<CodeBlockTabs defaultValue="javascript" groupId="lists-length">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/length/jobs`, {
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      }
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .get(&format!("{}/{}/lists/length/jobs", base_url, plate_id))
            .header("Authorization", api_key)
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	req, _ := http.NewRequest("GET", fmt.Sprintf("%s/%s/lists/length/jobs", baseUrl, plateId), nil)
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X GET "[base-url]/[id]/lists/length/jobs" \
      -H "Authorization: YOUR_API_KEY"
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Find Position (LPOS)

<CodeBlockTabs defaultValue="javascript" groupId="lists-position">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/position`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "jobs",
        value: "urgent",
        rank: 1,
        count: 5
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/position", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "jobs",
                "value": "urgent",
                "rank": 1,
                "count": 5
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"jobs","value":"urgent","rank":1,"count":5}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/position", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/position" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "jobs",
        "value": "urgent",
        "rank": 1,
        "count": 5
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

Query parameters (via JSON body):
- `rank` (optional): Start searching from this position
- `count` (optional): Number of positions to return
- `maxlen` (optional): Limit search depth

### Set Item by Index

<CodeBlockTabs defaultValue="javascript" groupId="lists-set">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/set`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "jobs",
        index: 0,
        value: "replaced"
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/set", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "jobs",
                "index": 0,
                "value": "replaced"
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"jobs","index":0,"value":"replaced"}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/set", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/set" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "jobs",
        "index": 0,
        "value": "replaced"
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Remove Items

<CodeBlockTabs defaultValue="javascript" groupId="lists-remove">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/remove`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "jobs",
        value: "done",
        count: 1
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/remove", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "jobs",
                "value": "done",
                "count": 1
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"jobs","value":"done","count":1}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/remove", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/remove" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "jobs",
        "value": "done",
        "count": 1
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

Set `count` to 0 to remove all matching items.

### Trim List

<CodeBlockTabs defaultValue="javascript" groupId="lists-trim">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/trim`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "jobs",
        start: 0,
        stop: 99
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/trim", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "jobs",
                "start": 0,
                "stop": 99
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"jobs","start":0,"stop":99}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/trim", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/trim" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "jobs",
        "start": 0,
        "stop": 99
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

Keeps only elements from index 0 to 99, removing everything after.

## Command Compatibility

<CodeBlockTabs defaultValue="javascript" groupId="lists-command">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/command`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        command: "LPUSH",
        args: ["mylist", "a", "b", "c"]
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/command", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "command": "LPUSH",
                "args": ["mylist", "a", "b", "c"]
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"command":"LPUSH","args":["mylist","a","b","c"]}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/command", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/command" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "command": "LPUSH",
        "args": ["mylist", "a", "b", "c"]
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

## Command Endpoints

### `POST /[id]/lists/command`

Execute allowed list commands across the plate.

**Allowed Commands:**

| Command | Description |
|---------|-------------|
| LPUSH | Push to left |
| RPUSH | Push to right |
| LPOP | Pop from left |
| RPOP | Pop from right |
| LLEN | Get length |
| LRANGE | Get range |
| LINDEX | Get by index |
| LPOS | Find position |
| LSET | Set by index |
| LINSERT | Insert relative to pivot |
| LREM | Remove items |
| LTRIM | Trim list |
| LMOVE | Move between lists |

**Query Parameters for LRANGE:**
- `start` (optional): Start index (default: 0)
- `stop` (optional): End index (default: -1)

**Query Parameters for LPOS:**
- `rank` (optional): Start position from this rank
- `count` (optional): Number of matches to return
- `maxlen` (optional): Limit search length

### `POST /[id]/lists/[key]/command`

Execute allowed commands on a specific list key.

**Allowed Commands:** Same as above.

**Request:**

<CodeBlockTabs defaultValue="javascript" groupId="lists-key-command">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/$[id]/lists/mylist/command`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        command: "LLEN"
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/lists/mylist/command", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "command": "LLEN"
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"command":"LLEN"}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/lists/mylist/command", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/lists/mylist/command" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "command": "LLEN"
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>
