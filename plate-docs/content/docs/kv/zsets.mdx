---
title: Sorted Sets API
description: Scored members for leaderboards, ranking, priorities, and time-based indexes
---

Sorted sets combine unique members with numeric scores, making them a good fit for leaderboards, ranking, priorities, and time-based indexes.

## Endpoints

| Method | Endpoint | Description |
| --- | --- | --- |
| POST | `/[id]/zsets/add` | Add scored members |
| POST | `/[id]/zsets/remove` | Remove members |
| GET | `/[id]/zsets/score/{key}/{member}` | Get a score |
| GET | `/[id]/zsets/rank/{key}/{member}` | Get a rank |
| GET | `/[id]/zsets/range/{key}` | Read a range |
| GET | `/[id]/zsets/count/{key}` | Count members |
| GET | `/[id]/zsets/count-by-score/{key}` | Count by score window |
| GET | `/[id]/zsets/count-by-lex/{key}` | Count by lex window |
| POST | `/[id]/zsets/increment` | Increment a score |
| POST | `/[id]/zsets/pop-min` | Pop lowest member(s) |
| POST | `/[id]/zsets/pop-max` | Pop highest member(s) |
| GET | `/[id]/zsets/random/{key}` | Read random member(s) |
| POST | `/[id]/zsets/scores` | Get multiple scores |
| POST | `/[id]/zsets/union` | Union-store |
| POST | `/[id]/zsets/intersect` | Intersect-store |
| POST | `/[id]/zsets/diff` | Diff-store |
| POST | `/[id]/zsets/range/store` | `ZRANGESTORE` |
| POST | `/[id]/zsets/command` | Execute allowed zset commands |
| POST | `/[id]/zsets/{key}/command` | Execute key-specific zset commands |

## Examples

### Add members

<CodeBlockTabs defaultValue="javascript" groupId="zsets-add">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/zsets/add`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "leaderboard",
        members: [
          { member: "player1", score: 100 },
          { member: "player2", score: 200 }
        ]
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/zsets/add", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "leaderboard",
                "members": [
                    { "member": "player1", "score": 100 },
                    { "member": "player2", "score": 200 }
                ]
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"leaderboard","members":[{"member":"player1","score":100},{"member":"player2","score":200}]}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/zsets/add", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/zsets/add" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "leaderboard",
        "members": [
          { "member": "player1", "score": 100 },
          { "member": "player2", "score": 200 }
        ]
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Read a range with scores

<CodeBlockTabs defaultValue="javascript" groupId="zsets-range">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/zsets/range/leaderboard?start=0&stop=9&with_scores=true`, {
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      }
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .get(&format!("{}/{}/zsets/range/leaderboard?start=0&stop=9&with_scores=true", base_url, plate_id))
            .header("Authorization", api_key)
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	req, _ := http.NewRequest("GET", fmt.Sprintf("%s/%s/zsets/range/leaderboard?start=0&stop=9&with_scores=true", baseUrl, plateId), nil)
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X GET "[base-url]/[id]/zsets/range/leaderboard?start=0&stop=9&with_scores=true" \
      -H "Authorization: YOUR_API_KEY"
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Read reverse rank

<CodeBlockTabs defaultValue="javascript" groupId="zsets-rank">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/zsets/rank/leaderboard/player2?order=desc`, {
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      }
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .get(&format!("{}/{}/zsets/rank/leaderboard/player2?order=desc", base_url, plate_id))
            .header("Authorization", api_key)
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	req, _ := http.NewRequest("GET", fmt.Sprintf("%s/%s/zsets/rank/leaderboard/player2?order=desc", baseUrl, plateId), nil)
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X GET "[base-url]/[id]/zsets/rank/leaderboard/player2?order=desc" \
      -H "Authorization: YOUR_API_KEY"
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Increment a score

<CodeBlockTabs defaultValue="javascript" groupId="zsets-increment">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/zsets/increment`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        key: "leaderboard",
        member: "player1",
        amount: 10
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/zsets/increment", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "key": "leaderboard",
                "member": "player1",
                "amount": 10
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"key":"leaderboard","member":"player1","amount":10}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/zsets/increment", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/zsets/increment" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "key": "leaderboard",
        "member": "player1",
        "amount": 10
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

### Store a weighted union

<CodeBlockTabs defaultValue="javascript" groupId="zsets-union">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/zsets/union`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        destination: "combined",
        keys: ["set:a", "set:b"],
        weights: [1, 2],
        aggregate: "sum"
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/zsets/union", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "destination": "combined",
                "keys": ["set:a", "set:b"],
                "weights": [1, 2],
                "aggregate": "sum"
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"destination":"combined","keys":["set:a","set:b"],"weights":[1,2],"aggregate":"sum"}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/zsets/union", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/zsets/union" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "destination": "combined",
        "keys": ["set:a", "set:b"],
        "weights": [1, 2],
        "aggregate": "sum"
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

## Command Compatibility

<CodeBlockTabs defaultValue="javascript" groupId="zsets-command">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/zsets/command`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        command: "ZADD",
        args: ["leaderboard", 100, "player1", 200, "player2"]
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/zsets/command", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "command": "ZADD",
                "args": ["leaderboard", 100, "player1", 200, "player2"]
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"command":"ZADD","args":["leaderboard",100,"player1",200,"player2"]}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/zsets/command", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/zsets/command" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "command": "ZADD",
        "args": ["leaderboard", 100, "player1", 200, "player2"]
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>

## Command Endpoints

### `POST /[id]/zsets/command`

Execute allowed sorted set commands across the plate.

**Allowed Commands:**

| Command | Description |
|---------|-------------|
| ZADD | Add scored members |
| ZREM | Remove members |
| ZSCORE | Get score |
| ZRANK | Get rank (ascending) |
| ZREVRANK | Get rank (descending) |
| ZRANGE | Get members by rank |
| ZCARD | Get member count |
| ZCOUNT | Count by score |
| ZLEXCOUNT | Count by lex |
| ZINCRBY | Increment score |
| ZPOPMIN | Pop lowest |
| ZPOPMAX | Pop highest |
| ZRANDMEMBER | Random member |
| ZUNIONSTORE | Union and store |
| ZINTERSTORE | Intersection and store |
| ZDIFFSTORE | Difference and store |
| ZRANGESTORE | Range and store |
| ZMSCORE | Get multiple scores |

**Query Parameters for ZRANGE:**
- `start` (optional): Start index (default: 0)
- `stop` (optional): End index (default: -1)
- `order` (optional): `asc` or `desc`
- `with_scores` (optional): Include scores in response

**Query Parameters for ZRANDMEMBER:**
- `count` (optional): Number of members
- `with_scores` (optional): Include scores

### `POST /[id]/zsets/[key]/command`

Execute allowed commands on a specific sorted set key.

**Allowed Commands:** Same as above.

**Request:**

<CodeBlockTabs defaultValue="javascript" groupId="zsets-key-command">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/zsets/leaderboard/command`, {
      method: "POST",
      headers: {
        "Authorization": apiKey,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        command: "ZCARD"
      })
    });

    const data = await response.json();
    console.log(data);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;
    use serde_json::json;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .post(&format!("{}/{}/zsets/leaderboard/command", base_url, plate_id))
            .header("Authorization", api_key)
            .json(&json!({
                "command": "ZCARD"
            }))
            .send()
            .await?;

        let data = response.json::<serde_json::Value>().await?;
        println!("{:?}", data);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"bytes"
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	payload := []byte(`{"command":"ZCARD"}`)
    	req, _ := http.NewRequest("POST", fmt.Sprintf("%s/%s/zsets/leaderboard/command", baseUrl, plateId), bytes.NewBuffer(payload))
    	req.Header.Set("Authorization", apiKey)
    	req.Header.Set("Content-Type", "application/json")

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X POST "[base-url]/[id]/zsets/leaderboard/command" \
      -H "Authorization: YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "command": "ZCARD"
      }'
    ```
  </CodeBlockTab>
</CodeBlockTabs>
