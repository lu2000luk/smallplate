---
title: Info API
description: Get statistics and information about your plate for monitoring
---

The Info API provides statistics and information about your plate - useful for monitoring and debugging!

## Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/{plateId}/info` | Get plate statistics |

## What Does Info Show?

The Info endpoint gives you:
- **keyCount**: Total number of keys in your plate
- **memoryUsageBytes**: Estimated memory used by your data

This helps you:
- Monitor storage usage
- Plan capacity
- Debug performance issues

## Get Plate Info

<CodeBlockTabs defaultValue="javascript" groupId="info-get">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="javascript">JavaScript</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="rust">Rust</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="go">Go</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="curl">cURL</CodeBlockTabsTrigger>
  </CodeBlockTabsList>

  <CodeBlockTab value="javascript">
    ```javascript
    const plateId = "[id]";
    const apiKey = "your-api-key";
    const baseUrl = "[base-url]";

    const response = await fetch(`${baseUrl}/${plateId}/info`, {
      method: "GET",
      headers: {
        "Authorization": apiKey
      }
    });

    const info = await response.json();
    // { keyCount: 150, memoryUsageBytes: 5242880 }
    console.log(info);
    ```
  </CodeBlockTab>

  <CodeBlockTab value="rust">
    ```rust
    use reqwest::Client;

    #[tokio::main]
    async fn main() -> Result<(), Box<dyn std::error::Error>> {
        let client = Client::new();
        let plate_id = "[id]";
        let api_key = "your-api-key";
        let base_url = "[base-url]";

        let response = client
            .get(&format!("{}/{}/info", base_url, plate_id))
            .header("Authorization", api_key)
            .send()
            .await?;

        let info = response.json::<serde_json::Value>().await?;
        println!("{:?}", info);
        Ok(())
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="go">
    ```go
    package main

    import (
    	"fmt"
    	"io"
    	"net/http"
    )

    func main() {
    	plateId := "[id]"
    	apiKey := "your-api-key"
    	baseUrl := "[base-url]"

    	req, _ := http.NewRequest("GET", fmt.Sprintf("%s/%s/info", baseUrl, plateId), nil)
    	req.Header.Set("Authorization", apiKey)

    	client := &http.Client{}
    	resp, _ := client.Do(req)
    	defer resp.Body.Close()

    	body, _ := io.ReadAll(resp.Body)
    	fmt.Println(string(body))
    }
    ```
  </CodeBlockTab>

  <CodeBlockTab value="curl">
    ```bash
    curl -X GET "[base-url]/[id]/info" \
      -H "Authorization: YOUR_API_KEY"
    ```
  </CodeBlockTab>
</CodeBlockTabs>

## Response Fields

| Field | Type | Description |
|-------|------|-------------|
| keyCount | number | Total number of keys in the plate |
| memoryUsageBytes | number | Estimated memory usage in bytes |

### Example Output

```json
{
  "keyCount": 150,
  "memoryUsageBytes": 5242880
}
```
